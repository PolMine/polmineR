library(polmineR)
use("polmineR")

testthat::context("subcorpus")

test_that(
  "generate subcorpus",
  {
    # c_size <- sum(c@cpos[,2] - c@cpos[,1]) + nrow(c@cpos)
    
  }
)


test_that(
  "methods for subcorpus objects",
  {
    
  }
)
